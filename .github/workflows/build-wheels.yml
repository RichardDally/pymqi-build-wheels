# Workflow to build pymqi wheels for different platforms and Python versions
name: Build Wheels

on:
  workflow_dispatch:
    inputs:
      pymqi_version:
        description: 'pymqi version to build (e.g., 1.12.10)'
        required: true
        type: string

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12', '3.13', '3.14']

    steps:
      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Download pymqi source
        run: |
          uv pip download --no-binary pymqi --no-deps pymqi==${{ inputs.pymqi_version }}
          tar -xzf pymqi-${{ inputs.pymqi_version }}.tar.gz
          mv pymqi-${{ inputs.pymqi_version }} pymqi-src
          cd pymqi-src
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Install system dependencies for pymqi on Linux
      - name: Install IBM MQ client (Linux)
        if: runner.os == 'Linux'
        run: |
          # Download and install IBM MQ client
          curl -LO https://public.dhe.ibm.com/ibmdl/export/pub/software/websphere/messaging/mqdev/redist/9.3.0.0-IBM-MQC-Redist-LinuxX64.tar.gz
          sudo mkdir -p /opt/mqm
          sudo tar -xzf 9.3.0.0-IBM-MQC-Redist-LinuxX64.tar.gz -C /opt/mqm
          echo "MQ_FILE_PATH=/opt/mqm" >> $GITHUB_ENV

      # Install system dependencies for pymqi on Windows
      - name: Install IBM MQ client (Windows)
        if: runner.os == 'Windows'
        shell: powershell
        run: |
          # Download IBM MQ client for Windows
          $url = "https://public.dhe.ibm.com/ibmdl/export/pub/software/websphere/messaging/mqdev/redist/9.3.0.0-IBM-MQC-Redist-Win64.zip"
          Invoke-WebRequest -Uri $url -OutFile mqclient.zip
          Expand-Archive -Path mqclient.zip -DestinationPath C:\mqm
          echo "MQ_FILE_PATH=C:\mqm" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Build wheel with uv
        working-directory: pymqi-src
        run: |
          uv pip install build wheel
          uv pip install -e .
          uv build --wheel

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: wheels-${{ matrix.os }}-py${{ matrix.python-version }}
          path: pymqi-src/dist/*.whl
          retention-days: 7

  # Job to create a GitHub Release with all wheels
  create_release:
    needs: [build_wheels]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all wheels
        uses: actions/download-artifact@v4
        with:
          pattern: wheels-*
          merge-multiple: true
          path: dist/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.pymqi_version }}
          name: pymqi v${{ inputs.pymqi_version }} wheels
          body: |
            Compiled wheels for pymqi version ${{ inputs.pymqi_version }}

            Supported platforms:
            - Linux (Ubuntu) x64
            - Windows x64

            Python versions:
            - Python 3.10
            - Python 3.11
            - Python 3.12
            - Python 3.13
            - Python 3.14
          files: dist/*.whl
          draft: false
          prerelease: false
